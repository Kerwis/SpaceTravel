namespace SpaceTravel.Game.UI
{
    public enum WindowId
    {
        None = 0,
        ShipView = 1,
        Storage = 2,
        Production = 3,
        Build = 4,
        // add more later (Drone, PvP, Settings, etc.)
    }
}